# Schritt 1: pygame laden und zeigen wie das Fenster aufgeht
# Schritt 2: Fenster bleibt und zeigt Titel Star Dodge

import pygame

WIDTH, HEIGHT = 1200, 800
pygame.display.set_mode((WIDTH, HEIGHT))
pygame.display.set_caption("Star Dodge")

"""
run = True

while run:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            run = False
            break

pygame.quit()
"""
